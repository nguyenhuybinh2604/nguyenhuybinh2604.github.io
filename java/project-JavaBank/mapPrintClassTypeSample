 for (Map.Entry<String, Object> entry : users.entrySet()) {
            String key = entry.getKey();
            Object value = entry.getValue();
            System.out.println(key + ": " + value + " (Class: " + value.getClass().getSimpleName() + ")");
        }